public interface PositionInterface {

    // getPoint method
    Point getPoint();

    // moveTo method
    void moveTo(double newX, double newY);

    // distanceTo method
    double distanceTo(double targetX,double  targetY);       // MUST FIX RETURN
}
